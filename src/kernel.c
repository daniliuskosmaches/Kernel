// src/kernel.c

// Эта функция будет вызываться из нашего ассемблерного кода
void kmain(void)
{
    // Пока что мы просто зацикливаемся, не делая ничего сложного
    // Здесь позже будет инициализация VGA, IDT и планировщика
    while (1) {
        // Простая бесконечная петля, чтобы ядро не завершилось
    }
}