#ifndef SHELL_H
#define SHELL_H

// ============================================================
// ЗАВИСИМОСТИ
// ============================================================
#include "stdint.h"
#include "stddef.h" // Для size_t и возможно других типов


// ============================================================
// ГЛАВНЫЙ ИНТЕРФЕЙС SHELL
// ============================================================

/**
 * Инициализирует и запускает командную оболочку.
 */
void shell_init(void);

/**
 * Обрабатывает один символ, введенный пользователем (например, с клавиатуры).
 * @param c: Введенный символ.
 */
void shell_handle_char(char c);


// ============================================================
// ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ (Требуются в shell.c для избежания implicit declaration)
// ============================================================

/**
 * Выводит приглашение командной строки (например, "myos> ").
 * * ИСПРАВЛЕНИЕ: Это объявление решает ошибку shell.c:33:5
 */
void shell_print_prompt(void);

/**
 * Парсит и выполняет команду.
 * * ИСПРАВЛЕНИЕ: Это объявление решает ошибку shell.c:53:13
 */
void shell_execute_command(const char* command);


// ============================================================
// ПРОТОТИПЫ КОМАНД (для их вызова, если они определены в shell.c)
// Эти команды могут быть статическими в shell.c, но если они вызываются из
//вне, их нужно объявить здесь.
// ============================================================

// Если команды объявлены как static, они не обязательно должны быть здесь,
// но для полноты и возможного использования извне, их можно добавить.
/*
void cmd_help(void);
void cmd_clear(void);
void cmd_echo(const char* args);
void cmd_meminfo(void);
void cmd_uptime(void);
void cmd_version(void);
*/

#endif // SHELL_H