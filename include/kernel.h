// kernel.h

#ifndef KERNEL_H
#define KERNEL_H

#include "stdint.h"
#include "stddef.h"

// ----------------------------------------------------
// 1. Прототипы главных функций ядра
// ----------------------------------------------------

// Главная функция ядра, вызываемая из ассемблера
void kernel_main(void);

// ----------------------------------------------------
// 2. Прототипы функций PMM (Управление физической памятью)
//    (Предполагаем, что вам нужна функция выделения памяти)
// ----------------------------------------------------

// Функция выделения одной страницы физической памяти
uint32_t pmm_alloc_page(void);

// Функция освобождения страницы физической памяти
void pmm_free_page(uint32_t addr);

// ----------------------------------------------------
// 3. Прототип kmalloc (Высокоуровневое выделение памяти)
//    (Предполагаем, что вы используете его в task.c)
// ----------------------------------------------------

// Функция выделения памяти в куче ядра
void* kmalloc(size_t size);

// ----------------------------------------------------
// 4. Прототипы VMM (Виртуальная память)
// ----------------------------------------------------





// ----------------------------------------------------
// 5. Прототипы IDT (Прерывания)
// ----------------------------------------------------

// Функция для установки IDT (если вам нужно ее вызвать)
void idt_install(void);


#endif